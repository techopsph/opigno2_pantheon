<div class="anonymous-slider list-unstyled d-flex">
  {% for item in items %}
    <div class="slider-item" style="background-image: url({{ item.img_path }})">
      <div class="slider-markup">{{ item.markup|raw }}</div>
    </div>
  {% endfor %}
</div>
